services:
  - type: web
    name: prep-guardian-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd src && uvicorn terminal_api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
